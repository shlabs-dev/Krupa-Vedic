services:
  krupa:
    build: .
    container_name: krupa
    restart: unless-stopped
    env_file: .env
    environment:
      DATABASE_URL: file:/app/data/krupa.db
    ports:
      - "127.0.0.1:3000:3000"
    volumes:
      - krupa-data:/app/data
volumes:
  krupa-data:
